import React, { ReactChildren } from "react";

export const HeaderTitle = () => {
  return (
    <div className="flex flex-col text-white text-center">
      <span className="font-semibold text-xl">Toby Hughes</span>
      <span className="text-white text-sm">Writing code that lasts</span>
    </div>
  );
};
